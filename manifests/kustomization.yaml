apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: hello-k8s
resources:
  - deployment.yaml
  - ns.yaml
  - service.yaml
secretGenerator:
  - name: secrets
    files:
      - secrets.enc.yaml
