apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: hello-k8s
resources:
  - deployment.yaml
  - ns.yaml
  - service.yaml
images:
  - name: registry.gitlab.com/klavionik/hello-k8s/app
    newTag: "1.0.0" # {"$imagepolicy": "flux-system:hello-k8s:tag"}
secretGenerator:
  - name: secrets
    envs:
      - secret.enc.env
